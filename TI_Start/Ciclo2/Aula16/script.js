//QuerySelector > retorna sempre o primeiro elemento de uma lista de elementos de uma determinada classe
//QuerySelectorAll > consulta de todos os seletores, retornará todos os elementos presentes no documento que estão no mesmo grupo de seletores
//forEach > para cada. É uma espécie de loop de repetição
//target > alvo
